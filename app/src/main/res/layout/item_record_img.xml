<?xml version="1.0" encoding="utf-8"?>
<androidx.constraintlayout.widget.ConstraintLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="wrap_content"
    android:layout_height="wrap_content"
    xmlns:app="http://schemas.android.com/apk/res-auto">

    <ImageView
        android:id="@+id/record_iv"
        android:layout_width="44.4dp"
        android:layout_height="44.4dp"
        android:layout_marginTop="2.4dp"
        android:layout_marginStart="15.6dp"
        android:scaleType="fitXY"
        android:background="@drawable/record_img_view"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintTop_toTopOf="@+id/remove_img_icon" />

    <View
        android:id="@+id/remove_img_view"
        android:layout_width="12dp"
        android:layout_height="12dp"
        android:background="@drawable/record_remove_img_view"
        app:layout_constraintTop_toTopOf="parent"
        app:layout_constraintStart_toStartOf="@+id/remove_img_icon"
        app:layout_constraintEnd_toEndOf="@+id/remove_img_icon" />

    <View
        android:id="@+id/remove_img_icon"
        android:layout_width="12dp"
        android:layout_height="12dp"
        android:layout_marginStart="36dp"
        android:background="@drawable/close_icon"
        app:layout_constraintStart_toStartOf="@+id/record_iv"
        app:layout_constraintTop_toTopOf="parent"/>

</androidx.constraintlayout.widget.ConstraintLayout>